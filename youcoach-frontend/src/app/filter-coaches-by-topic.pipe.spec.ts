import { FilterCoachesByTopicPipe } from './filter-coaches-by-topic.pipe';

describe('FilterCoachesByTopicPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCoachesByTopicPipe();
    expect(pipe).toBeTruthy();
  });
});
