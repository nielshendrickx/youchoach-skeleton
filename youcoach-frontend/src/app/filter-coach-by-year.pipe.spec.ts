import { FilterCoachesByYearPipe } from './filter-coach-by-year.pipe';

describe('FilterCoachByYearPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCoachesByYearPipe();
    expect(pipe).toBeTruthy();
  });
});
