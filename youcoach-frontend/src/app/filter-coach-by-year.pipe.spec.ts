import { FilterCoachByYearPipe } from './filter-coach-by-year.pipe';

describe('FilterCoachByYearPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCoachByYearPipe();
    expect(pipe).toBeTruthy();
  });
});
